find floor of(10^n)=A
->find floor of (A/X)%X== (A/X-KX)
->find the (10^N)%X^2
->(A%X^2)==(A-KX^2=D)
now divide both side by X
-> we will get (A/X-KX) which will be our answer

Implementation:
-> we just have mod 10^N with X^2
->then divide by X just as before equation